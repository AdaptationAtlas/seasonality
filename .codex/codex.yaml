workspace:
  allow_file_edit: true
  allow_shell: true
  allow_git: true

commands:
  test: "Rscript tests/run_tests.R"
  lint: "Rscript -e 'lintr::lint_dir(c(\"R\", \"scripts\", \"tests\"))'"
  preflight: "Rscript scripts/preflight.R"

safety:
  confirm_destructive: true
